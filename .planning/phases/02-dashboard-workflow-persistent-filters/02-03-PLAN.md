---
phase: 02-dashboard-workflow-persistent-filters
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/pages/index.tsx
  - frontend/src/pages/total.tsx
  - frontend/src/components/RunMap/RunMapButtons.tsx
  - frontend/src/components/LocationStat/index.tsx
  - frontend/src/components/YearsStat/index.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - Dashboard and detail-related routes consume the same shared filters and stay in sync when navigating.
    - Browser back/forward replays filter history from URL for the active view.
    - User-selected filters persist across reload and route changes without silent reset.
  artifacts:
    - path: frontend/src/pages/index.tsx
      provides: Shared-filter-driven dashboard interactions and URL replay behavior
      contains: "useDashboardFilters"
    - path: frontend/src/pages/total.tsx
      provides: Detail-related route consumption of shared filter defaults/state
      contains: "useDashboardFilters"
    - path: frontend/src/components/RunMap/RunMapButtons.tsx
      provides: Year control wired to global filter state instead of local-only hook reads
      contains: "changeYear"
  key_links:
    - from: frontend/src/components/RunMap/RunMapButtons.tsx
      to: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      via: year selection dispatch
      pattern: "useDashboardFilters\\("
    - from: frontend/src/pages/total.tsx
      to: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      via: shared filter consumption
      pattern: "useDashboardFilters\\("
    - from: frontend/src/pages/index.tsx
      to: window.history
      via: query/hash coexistence updates
      pattern: "window\\.history\\.(pushState|replaceState)"
---

<objective>
Complete cross-view synchronization by migrating existing filter controls and detail-related route consumers onto the shared filter model with URL replay semantics.

Purpose: Finish DASH-02 and DASH-03 user workflow continuity across navigation and route context changes.
Output: Filter-control wiring updates for dashboard/detail routes and shared-state navigation persistence behavior.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-CONTEXT.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-RESEARCH.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-01-PLAN.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-02-PLAN.md
@frontend/src/pages/index.tsx
@frontend/src/pages/total.tsx
@frontend/src/components/RunMap/RunMapButtons.tsx
@frontend/src/components/LocationStat/index.tsx
@frontend/src/components/YearsStat/index.tsx

<interfaces>
From frontend/src/features/dashboard/filters/useDashboardFilters.tsx (Plan 02-01 output):
```typescript
export function useDashboardFilters(): {
  filters: DashboardFilters;
  setYear: (year: string) => void;
  setActivityType: (type: string) => void;
  setCity: (city: string) => void;
  setTitle: (title: string) => void;
  replaceFilters: (next: DashboardFilters) => void;
};
```

From frontend/src/components/RunMap/RunMapButtons.tsx:
```typescript
const RunMapButtons = ({ changeYear, thisYear }: {
  changeYear: (_year: string) => void;
  thisYear: string;
}) => { ... };
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate existing filter controls to consume global filter state/actions</name>
  <read_first>@frontend/src/components/RunMap/RunMapButtons.tsx, @frontend/src/components/LocationStat/index.tsx, @frontend/src/components/YearsStat/index.tsx</read_first>
  <files>frontend/src/components/RunMap/RunMapButtons.tsx, frontend/src/components/LocationStat/index.tsx, frontend/src/components/YearsStat/index.tsx, frontend/src/pages/index.tsx</files>
  <action>Refactor year/city/title controls so they consume centralized filter state/actions rather than calling `useActivities()` directly for selection state. Keep existing visual components but change data/event wiring to shared filter hook. Ensure changing any control updates URL immediately and does not desynchronize controls from current view state. Preserve locked behavior: filter changes that exclude focused single run must clear run focus/hash and show filtered set.</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `RunMapButtons` no longer derives active year from independent local state only.
    - `LocationStat`/`YearsStat` interactions dispatch into shared filter actions.
    - `index.tsx` is the single owner of hash-clearing rule when filters exclude focused run.
  </acceptance_criteria>
  <done>All dashboard filter entry points operate on one shared global model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire detail-related `/summary` route to shared filter model for cross-view continuity</name>
  <read_first>@frontend/src/pages/total.tsx, @frontend/src/components/ActivityList/index.tsx, @frontend/src/features/dashboard/filters/useDashboardFilters.tsx</read_first>
  <files>frontend/src/pages/total.tsx</files>
  <behavior>
    - Navigating between `/` and `/summary` preserves current shared filter selections.
    - `/summary` can consume shared filter defaults/state without resetting global filter store.
    - Reloading `/summary` with URL filters hydrates to same state as `/`.
  </behavior>
  <action>Update `total.tsx` to consume `useDashboardFilters()` and align page behavior with shared global filter state (minimum scope per research recommendation: shared defaults and state continuity, not full ActivityList rewrite). Keep existing `ActivityList` rendering intact, but ensure route-level mount does not initialize conflicting filter state and can honor existing URL query filter params.</action>
  <verify>
    <automated>cd frontend && pnpm run test -- src/features/dashboard/filters/sharedFilterModel.test.tsx src/features/dashboard/filters/persistence.test.ts && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `total.tsx` imports `useDashboardFilters`.
    - Route transitions do not clear shared filter state unexpectedly.
    - URL-driven filter hydration applies consistently when landing on `/summary`.
  </acceptance_criteria>
  <done>Detail-related route participates in shared filter continuity model.</done>
</task>

</tasks>

<verification>
Run `cd frontend && pnpm run test -- src/features/dashboard/filters/sharedFilterModel.test.tsx src/features/dashboard/filters/persistence.test.ts src/features/dashboard/components/KpiCards.test.tsx && pnpm run build` and confirm pass.
</verification>

<success_criteria>
- Shared filters remain stable across dashboard/detail route navigation.
- URL history and reload preserve filter intent consistently.
- Existing controls operate through centralized orchestration with no view drift.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-workflow-persistent-filters/02-03-SUMMARY.md`
</output>

