---
phase: 02-dashboard-workflow-persistent-filters
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - frontend/src/features/dashboard/selectors/selectFilteredRuns.ts
  - frontend/src/features/dashboard/selectors/selectKpis.ts
  - frontend/src/features/dashboard/components/KpiCards.tsx
  - frontend/src/features/dashboard/components/EmptyKpiState.tsx
  - frontend/src/features/dashboard/components/KpiCards.test.tsx
  - frontend/src/pages/index.tsx
autonomous: true
requirements:
  - DASH-01
  - DASH-02
must_haves:
  truths:
    - User sees four KPI cards (distance, duration, run count, average heart rate) based on current shared filters.
    - KPI cards render in one compact row on desktop and two-column wrapped layout on mobile while preserving KPI order.
    - When filters produce zero runs, user sees explicit empty-state card and current filters remain unchanged.
  artifacts:
    - path: frontend/src/features/dashboard/selectors/selectFilteredRuns.ts
      provides: Shared filtered-run derivation from activities + global filter state
      exports:
        - selectFilteredRuns
    - path: frontend/src/features/dashboard/selectors/selectKpis.ts
      provides: KPI aggregation for distance/duration/runs/average HR
      exports:
        - selectKpis
    - path: frontend/src/features/dashboard/components/KpiCards.tsx
      provides: KPI card UI with locked desktop/mobile density behavior
      exports:
        - KpiCards
    - path: frontend/src/features/dashboard/components/EmptyKpiState.tsx
      provides: Explicit zero-results UI state without filter reset
      exports:
        - EmptyKpiState
  key_links:
    - from: frontend/src/pages/index.tsx
      to: frontend/src/features/dashboard/selectors/selectFilteredRuns.ts
      via: filtered dataset computation
      pattern: "selectFilteredRuns\\("
    - from: frontend/src/pages/index.tsx
      to: frontend/src/features/dashboard/components/KpiCards.tsx
      via: KPI rendering call
      pattern: "<KpiCards"
    - from: frontend/src/features/dashboard/selectors/selectKpis.ts
      to: frontend/src/utils/utils.ts
      via: moving-time conversion
      pattern: "convertMovingTime2Sec"
---

<objective>
Deliver the phase KPI dashboard surface on top of the shared filter model, including zero-result handling and locked responsive layout behavior.

Purpose: Satisfy DASH-01 UI outcome while consuming DASH-02 centralized filter contracts from Plan 02-01.
Output: KPI selectors + KPI/empty-state components + index-page wiring to shared filtered dataset.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-CONTEXT.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-RESEARCH.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-01-PLAN.md
@frontend/src/pages/index.tsx
@frontend/src/hooks/useActivities.ts
@frontend/src/utils/utils.ts

<interfaces>
From frontend/src/features/dashboard/filters/useDashboardFilters.tsx (Plan 02-01 output):
```typescript
export function useDashboardFilters(): {
  filters: DashboardFilters;
  setYear: (year: string) => void;
  setActivityType: (type: string) => void;
  setCity: (city: string) => void;
  setTitle: (title: string) => void;
  replaceFilters: (next: DashboardFilters) => void;
};
```

From frontend/src/utils/utils.ts:
```typescript
export const convertMovingTime2Sec: (moving_time: string) => number;
export interface Activity {
  distance: number;
  moving_time: string;
  average_heartrate?: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement filtered-run and KPI selectors from shared filter contracts</name>
  <read_first>@frontend/src/features/dashboard/filters/model.ts, @frontend/src/utils/utils.ts, @frontend/src/pages/index.tsx</read_first>
  <files>frontend/src/features/dashboard/selectors/selectFilteredRuns.ts, frontend/src/features/dashboard/selectors/selectKpis.ts</files>
  <action>Create selector utilities that accept raw `Activity[]` plus centralized `DashboardFilters` and return (1) filtered runs, (2) KPI aggregate payload. Ensure filters support all baseline dimensions (all-time/year/date mode + activity type + city + title). KPI output must include total distance, total duration seconds, run count, and average HR (nullable when no HR data). Reuse existing utilities like `convertMovingTime2Sec`; do not duplicate parsing logic in UI components.</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `selectFilteredRuns.ts` exports one pure selector that consumes `DashboardFilters`.
    - `selectKpis.ts` exports one pure selector returning distance/duration/runCount/avgHr.
    - No KPI math is left inline in `index.tsx`.
  </acceptance_criteria>
  <done>Dashboard KPI behavior is driven by reusable selectors rather than page-local ad-hoc math.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build KPI cards + explicit empty-state components with locked responsive layout</name>
  <read_first>@.planning/phases/02-dashboard-workflow-persistent-filters/02-CONTEXT.md, @frontend/src/styles/index.css</read_first>
  <files>frontend/src/features/dashboard/components/KpiCards.tsx, frontend/src/features/dashboard/components/EmptyKpiState.tsx, frontend/src/features/dashboard/components/KpiCards.test.tsx</files>
  <behavior>
    - Four KPI cards always render in required order: distance, duration, runs, average heart rate.
    - Desktop shows one compact row of four cards; mobile wraps to two columns while preserving order.
    - Zero-result state renders explicit empty-state card and does not auto-reset filters.
  </behavior>
  <action>Write `KpiCards.test.tsx` first for ordering, value rendering, and empty-state behavior, then implement `KpiCards.tsx` + `EmptyKpiState.tsx`. Keep cards value-only per locked decision (no delta arrows, sparklines, or trend badges). Use existing style system and utility classes to implement compact desktop row + two-column mobile wrap. Make empty-state copy explicit and instructional while preserving current filter context.</action>
  <verify>
    <automated>cd frontend && pnpm run test -- src/features/dashboard/components/KpiCards.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Tests assert KPI order and value-only rendering.
    - Component includes responsive layout classes implementing 4-up desktop and 2-col mobile wrap.
    - Empty state component is used when filtered run count is zero.
  </acceptance_criteria>
  <done>KPI UI contract is implemented and regression-tested for layout/order/empty behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire index page to shared selectors and KPI components without breaking existing map/table flow</name>
  <read_first>@frontend/src/pages/index.tsx, @frontend/src/features/dashboard/filters/useDashboardFilters.tsx, @frontend/src/features/dashboard/selectors/selectFilteredRuns.ts</read_first>
  <files>frontend/src/pages/index.tsx</files>
  <behavior>
    - Index page consumes shared global filters and derives runs via selector utilities.
    - KPI cards update immediately when shared filters change.
    - If active single-run focus is excluded by new filters, page exits single-run focus and shows filtered set.
  </behavior>
  <action>Refactor `index.tsx` to consume `useDashboardFilters()` and replace page-local `currentFilter` calculations with shared selector usage. Render KPI cards near top of dashboard view. Keep existing run hash behavior but add guard: when filtered runs no longer include `singleRunId`, clear single-run focus/hash and display the filtered list/map. Preserve existing map/table components and interactions; this task is wiring, not redesign.</action>
  <verify>
    <automated>cd frontend && pnpm run test -- src/features/dashboard/components/KpiCards.test.tsx src/features/dashboard/filters/sharedFilterModel.test.tsx && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `index.tsx` imports and uses shared filter hook + selectors.
    - `index.tsx` renders `KpiCards` and `EmptyKpiState` based on filtered output.
    - Single-run exclusion rule is handled explicitly when filters change.
  </acceptance_criteria>
  <done>Main dashboard now surfaces modern KPI cards tied to shared persistent filters and resilient empty-state behavior.</done>
</task>

</tasks>

<verification>
Run `cd frontend && pnpm run test -- src/features/dashboard/components/KpiCards.test.tsx src/features/dashboard/filters/sharedFilterModel.test.tsx src/features/dashboard/filters/persistence.test.ts && pnpm run build` and confirm green.
</verification>

<success_criteria>
- Dashboard shows the locked KPI set for the currently filtered dataset.
- KPI layout matches locked desktop/mobile density rules.
- Empty-result state is explicit and filter-preserving.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-workflow-persistent-filters/02-02-SUMMARY.md`
</output>

