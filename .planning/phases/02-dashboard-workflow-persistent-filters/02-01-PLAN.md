---
phase: 02-dashboard-workflow-persistent-filters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/features/dashboard/filters/model.ts
  - frontend/src/features/dashboard/filters/serialize.ts
  - frontend/src/features/dashboard/filters/reducer.ts
  - frontend/src/features/dashboard/filters/useDashboardFilters.tsx
  - frontend/src/features/dashboard/filters/persistence.test.ts
  - frontend/src/features/dashboard/filters/sharedFilterModel.test.tsx
  - frontend/src/main.tsx
autonomous: true
requirements:
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - User filter state is managed by one centralized model instead of per-component islands.
    - URL query params are the primary filter source, and localStorage is only fallback when URL params are absent.
    - Invalid/stale persisted filter values are sanitized and app rendering continues safely.
    - Browser back/forward replays exact historical filter state from URL.
  artifacts:
    - path: frontend/src/features/dashboard/filters/model.ts
      provides: Canonical dashboard filter type contracts, defaults, and sanitizers
      exports:
        - DashboardFilters
        - DEFAULT_DASHBOARD_FILTERS
        - sanitizeDashboardFilters
    - path: frontend/src/features/dashboard/filters/serialize.ts
      provides: URL/localStorage encode/decode + precedence hydration helpers
      exports:
        - decodeFiltersFromUrl
        - encodeFiltersToUrl
        - hydrateFilters
    - path: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      provides: Shared provider/hook API for dashboard filter orchestration
      exports:
        - DashboardFiltersProvider
        - useDashboardFilters
  key_links:
    - from: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      to: window.location.search
      via: URL sync effect with equality guard
      pattern: "window\\.history\\.(pushState|replaceState)"
    - from: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      to: localStorage
      via: fallback persistence write/read
      pattern: "localStorage\\.getItem|localStorage\\.setItem"
    - from: frontend/src/main.tsx
      to: frontend/src/features/dashboard/filters/useDashboardFilters.tsx
      via: app-root provider wiring
      pattern: "DashboardFiltersProvider"
---

<objective>
Build the centralized filter orchestration foundation so dashboard/detail surfaces share one deterministic filter state with URL-first persistence.

Purpose: Lock down DASH-02 and DASH-03 rules before UI wiring so downstream plans can consume stable contracts.
Output: Shared filter contracts, reducer/provider, URL/localStorage hydration, and test coverage for precedence/replay/sanitization.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-CONTEXT.md
@.planning/phases/02-dashboard-workflow-persistent-filters/02-RESEARCH.md
@.planning/phases/01-typed-api-foundation/01-02-SUMMARY.md
@frontend/src/main.tsx
@frontend/src/pages/index.tsx
@frontend/src/utils/utils.ts

<interfaces>
From frontend/src/utils/utils.ts:
```typescript
export interface Activity {
  run_id: number;
  name: string;
  distance: number;
  moving_time: string;
  type: string;
  start_date_local: string;
  location_country?: string | null;
  average_heartrate?: number | null;
}
```

From frontend/src/main.tsx:
```typescript
const routes = createBrowserRouter([...], { basename: import.meta.env.BASE_URL });
ReactDOM.createRoot(document.getElementById('root')!).render(
  <HelmetProvider>
    <QueryClientProvider client={queryClient}>
      <RouterProvider router={routes} />
    </QueryClientProvider>
  </HelmetProvider>
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define shared filter contracts, defaults, and sanitizers (interface-first)</name>
  <read_first>@.planning/phases/02-dashboard-workflow-persistent-filters/02-CONTEXT.md, @frontend/src/utils/utils.ts</read_first>
  <files>frontend/src/features/dashboard/filters/model.ts, frontend/src/features/dashboard/filters/reducer.ts</files>
  <action>Create `model.ts` with exported `DashboardFilters` contract and explicit dimensions required by locked decisions: date range defaulting to literal all-time, activity type, year, city bucket, and title bucket. Include deterministic defaults and sanitizer functions that clamp unknown/stale values to safe defaults (never throw). Create `reducer.ts` with typed actions for each dimension and a single replace/hydrate action used by URL replay. Do not add deferred functionality (no trend toggles, no animation controls, no HR methodology UI flags).</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `model.ts` exports `DashboardFilters`, `DEFAULT_DASHBOARD_FILTERS`, and `sanitizeDashboardFilters`.
    - Defaults encode "All time" as full dataset behavior (not current-year alias).
    - `reducer.ts` exposes typed actions for year, activity type, city, title, and hydrate/replace.
  </acceptance_criteria>
  <done>Downstream plans have a stable typed filter contract and reducer API to implement against.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement URL/localStorage serialization-hydration with precedence and replay tests</name>
  <read_first>@frontend/src/features/dashboard/filters/model.ts, @.planning/phases/02-dashboard-workflow-persistent-filters/02-RESEARCH.md</read_first>
  <files>frontend/src/features/dashboard/filters/serialize.ts, frontend/src/features/dashboard/filters/persistence.test.ts</files>
  <behavior>
    - URL params override localStorage values whenever URL carries filter keys.
    - When URL has no filter keys, localStorage fallback hydrates and is sanitized.
    - Back/forward replay decodes historical URL state deterministically.
    - Invalid query/storage values never crash and resolve to safe defaults.
  </behavior>
  <action>Write failing tests in `persistence.test.ts` first, then implement serializer helpers in `serialize.ts` using native `URLSearchParams` (no extra dependency). Provide helpers for encode/decode/hydrate and a stable storage key with version suffix. Add equality guard helper for serialized state to prevent infinite URL sync loops. Preserve hash handling contract by ensuring search-string updates do not unintentionally drop existing hash unless caller explicitly clears it.</action>
  <verify>
    <automated>cd frontend && pnpm run test -- src/features/dashboard/filters/persistence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Test suite covers URL-first precedence, localStorage fallback, stale value sanitization, and replay behavior.
    - `serialize.ts` uses `URLSearchParams` and exports encode/decode/hydrate helpers.
    - Storage key is versioned (example: `dashboard.filters.v1`) to support future migrations.
  </acceptance_criteria>
  <done>Persistence semantics are executable and regression-tested before UI consumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build shared provider/hook and wire it at app root with integration test</name>
  <read_first>@frontend/src/main.tsx, @frontend/src/features/dashboard/filters/model.ts, @frontend/src/features/dashboard/filters/serialize.ts</read_first>
  <files>frontend/src/features/dashboard/filters/useDashboardFilters.tsx, frontend/src/features/dashboard/filters/sharedFilterModel.test.tsx, frontend/src/main.tsx</files>
  <behavior>
    - Dashboard and detail consumers read/write one global filter state from the same provider.
    - Filter writes update URL immediately with deterministic query keys.
    - Popstate/back-forward rehydrates provider state from URL without local drift.
  </behavior>
  <action>Create `DashboardFiltersProvider` and `useDashboardFilters` hook using `useReducer` plus serializer helpers. Wire provider in `main.tsx` so both `/` and `/summary` are inside the same provider scope. Add integration tests in `sharedFilterModel.test.tsx` proving two consumers stay synchronized and popstate replay works. Keep implementation centralized and avoid per-page duplicate state initialization.</action>
  <verify>
    <automated>cd frontend && pnpm run test -- src/features/dashboard/filters/sharedFilterModel.test.tsx src/features/dashboard/filters/persistence.test.ts && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - Provider and hook exports are available from `useDashboardFilters.tsx`.
    - `main.tsx` wraps router with `DashboardFiltersProvider`.
    - Integration test demonstrates synchronized state across at least two mock consumers.
  </acceptance_criteria>
  <done>Centralized shared filter orchestration exists and is bootstrapped for all route consumers.</done>
</task>

</tasks>

<verification>
Run `cd frontend && pnpm run test -- src/features/dashboard/filters/persistence.test.ts src/features/dashboard/filters/sharedFilterModel.test.tsx && pnpm run build` and confirm all checks pass.
</verification>

<success_criteria>
- One canonical filter state exists for route consumers.
- URL-first + localStorage-fallback behavior is automated and tested.
- Back/forward replay and sanitization behavior are deterministic and regression-protected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-workflow-persistent-filters/02-01-SUMMARY.md`
</output>

