---
phase: 01-typed-api-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - frontend/package.json
  - frontend/src/api/contracts/schema.d.ts
  - frontend/src/api/client.ts
  - frontend/src/api/activities.ts
  - frontend/src/api/analytics.ts
  - frontend/src/hooks/useActivities.ts
  - frontend/src/main.tsx
  - frontend/src/api/types.ts
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - Frontend reads activity/analytics data via generated API contract types rather than direct static JSON assumptions.
    - Existing activity hook keeps behavior parity through incremental API-first migration with explicit fallback.
    - Freshness/completeness metadata from API is available at the hook/client boundary for downstream UI trust signals.
  artifacts:
    - path: frontend/src/api/contracts/schema.d.ts
      provides: Generated TypeScript contract types from backend OpenAPI
      contains: "paths"
    - path: frontend/src/api/client.ts
      provides: Typed API fetch wrapper bound to `/api/v1`
      exports:
        - apiClient
    - path: frontend/src/hooks/useActivities.ts
      provides: API-backed hook boundary with static-data fallback
      contains: "useActivities"
  key_links:
    - from: frontend/src/api/contracts/schema.d.ts
      to: backend/api/openapi.json
      via: openapi-typescript generation
      pattern: "generate:api-types"
    - from: frontend/src/hooks/useActivities.ts
      to: frontend/src/api/activities.ts
      via: typed fetch call
      pattern: "getActivities\\("
    - from: frontend/src/main.tsx
      to: "@tanstack/react-query"
      via: provider wiring
      pattern: "QueryClientProvider"
---

<objective>
Wire frontend typed API consumption using generated contracts and migrate the activity data boundary away from direct JSON coupling while preserving behavior parity.

Purpose: Complete API-01 consumption path and expose API-02 metadata to frontend boundaries.
Output: Generated TS contracts + typed API client modules + migrated `useActivities` hook.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-typed-api-foundation/01-CONTEXT.md
@.planning/phases/01-typed-api-foundation/01-RESEARCH.md
@.planning/phases/01-typed-api-foundation/01-01-PLAN.md
@backend/api/openapi.json
@frontend/src/hooks/useActivities.ts
@frontend/src/main.tsx
@frontend/src/utils/utils.ts

<interfaces>
From frontend/src/utils/utils.ts:
```typescript
export interface Activity {
  run_id: number;
  name: string;
  distance: number;
  moving_time: string;
  type: string;
  subtype: string;
  start_date: string;
  start_date_local: string;
  location_country?: string | null;
  summary_polyline?: string | null;
  average_heartrate?: number | null;
  elevation_gain: number | null;
  average_speed: number;
  streak: number;
}
```

From frontend/src/main.tsx:
```typescript
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HelmetProvider>
      <RouterProvider router={routes} />
    </HelmetProvider>
  </React.StrictMode>
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate frontend contract types and typed API client boundary</name>
  <read_first>@backend/api/openapi.json, @frontend/package.json</read_first>
  <files>frontend/package.json, frontend/src/api/contracts/schema.d.ts, frontend/src/api/client.ts, frontend/src/api/types.ts</files>
  <action>Add frontend tooling for contract consumption: include `openapi-typescript@7.13.0` in devDependencies and script `generate:api-types` that reads `../backend/api/openapi.json` and writes `src/api/contracts/schema.d.ts`. Create `frontend/src/api/client.ts` with typed JSON fetch helper targeting `/api/v1` endpoints and deterministic error handling for non-2xx responses. Add `frontend/src/api/types.ts` adapters that map generated contract types to the existing `Activity` interface shape expected by current components (no `any`, no runtime shape guessing).</action>
  <verify>
    <automated>cd frontend && pnpm run generate:api-types && rg "export interface paths|export type paths" src/api/contracts/schema.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "generate:api-types" frontend/package.json` returns one script entry.
    - `rg "openapi-typescript ../backend/api/openapi.json" frontend/package.json` returns one match.
    - `rg "export (const|async function) apiClient" frontend/src/api/client.ts` returns one or more matches.
  </acceptance_criteria>
  <done>Frontend has reproducible generated contract types and a typed API client boundary rooted in backend OpenAPI output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate `useActivities` to typed API-first flow with static fallback</name>
  <read_first>@frontend/src/hooks/useActivities.ts, @frontend/src/utils/utils.ts, @frontend/src/pages/index.tsx</read_first>
  <files>frontend/src/api/activities.ts, frontend/src/api/analytics.ts, frontend/src/hooks/useActivities.ts</files>
  <behavior>
    - Hook reads activities through typed API module first and preserves existing derived values (`years`, `countries`, `provinces`, `cities`, `runPeriod`, `thisYear`).
    - Hook falls back to local static JSON path when API call fails or feature flag disables API path.
    - Hook captures freshness metadata alongside activity payload for downstream UI usage.
  </behavior>
  <action>Create `frontend/src/api/activities.ts` and `frontend/src/api/analytics.ts` wrappers that call typed `apiClient` and return parsed contract payloads including freshness metadata. Refactor `useActivities.ts` into API-first incremental migration: use API result when available, otherwise reuse legacy static JSON import path to preserve current behavior. Keep return shape backward-compatible for `pages/index.tsx` to avoid cross-cutting page rewrites in this phase; add metadata field in hook return without removing existing fields.</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "getActivities\\(|/api/v1/activities" frontend/src/hooks/useActivities.ts frontend/src/api/activities.ts` returns matches.
    - `rg "import activities from '@data/activities.json'" frontend/src/hooks/useActivities.ts` returns one match (fallback retained).
    - `rg "freshness" frontend/src/hooks/useActivities.ts frontend/src/api/activities.ts` returns matches.
  </acceptance_criteria>
  <done>`useActivities` is no longer static-JSON-only; it consumes typed API contracts first while preserving existing UI behavior with fallback.</done>
</task>

<task type="auto">
  <name>Task 3: Wire TanStack Query provider for typed API server-state usage</name>
  <read_first>@frontend/src/main.tsx</read_first>
  <files>frontend/src/main.tsx</files>
  <action>Install and wire `@tanstack/react-query@5.91.3` in `frontend/src/main.tsx` by creating a single `QueryClient` instance and wrapping `RouterProvider` with `QueryClientProvider`. Keep existing Helmet and router behavior intact. This prepares consistent typed API loading/error/cache behavior for downstream phases without introducing UI redesign work in this phase.</action>
  <verify>
    <automated>cd frontend && pnpm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "QueryClientProvider" frontend/src/main.tsx` returns one or more matches.
    - `rg "new QueryClient\\(" frontend/src/main.tsx` returns one match.
    - `rg "@tanstack/react-query" frontend/package.json` returns dependency entry.
  </acceptance_criteria>
  <done>App bootstrap includes React Query provider so typed API hooks can share consistent server-state behavior.</done>
</task>

</tasks>

<verification>
Run `cd frontend && pnpm run generate:api-types && pnpm run build` to confirm generated contracts are consumed and the app compiles with migrated typed hook boundary.
</verification>

<success_criteria>
- Frontend type contracts are generated from backend OpenAPI output, not hand-authored DTO guesses.
- `useActivities` consumes typed API path and still supports static fallback during migration.
- Freshness/completeness metadata is available at frontend data boundary for downstream trust signaling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-typed-api-foundation/01-02-SUMMARY.md`
</output>
