---
phase: 01-typed-api-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/api/main.py
  - backend/api/routers/activities.py
  - backend/api/routers/analytics.py
  - backend/api/schemas/common.py
  - backend/api/schemas/activity.py
  - backend/api/schemas/analytics.py
  - backend/api/services/activity_service.py
  - backend/api/services/analytics_service.py
  - backend/api/export_openapi.py
  - backend/tests/conftest.py
  - backend/tests/api/test_contracts.py
  - backend/tests/api/test_freshness.py
  - backend/pytest.ini
autonomous: true
requirements:
  - API-01
  - API-02
must_haves:
  truths:
    - User-facing data endpoints are served from a versioned `/api/v1` boundary.
    - Activity and analytics responses always include freshness metadata with `last_sync_at` and `completeness`.
    - Contract test coverage prevents schema drift for API responses used by frontend views.
  artifacts:
    - path: backend/api/main.py
      provides: FastAPI app mounting `/api/v1` routers
      exports:
        - app
    - path: backend/api/schemas/common.py
      provides: Shared freshness/completeness typed envelope
      contains: "class FreshnessMetadata(BaseModel)"
    - path: backend/tests/api/test_contracts.py
      provides: API-01 contract regression tests
      contains: "def test_activities_contract"
    - path: backend/tests/api/test_freshness.py
      provides: API-02 freshness metadata tests
      contains: "def test_freshness_fields_required"
  key_links:
    - from: backend/api/routers/activities.py
      to: backend/api/schemas/activity.py
      via: response_model
      pattern: "response_model=ActivitiesResponse"
    - from: backend/api/routers/analytics.py
      to: backend/api/schemas/common.py
      via: shared FreshnessMetadata usage
      pattern: "freshness=FreshnessMetadata"
    - from: backend/tests/api/test_freshness.py
      to: backend/api/main.py
      via: TestClient app import
      pattern: "from api.main import app"
---

<objective>
Create the typed backend API contract foundation for Phase 1 by introducing `/api/v1` endpoints, mandatory freshness/completeness metadata, and contract tests.

Purpose: Establish a stable source-of-truth contract boundary before frontend migration.
Output: FastAPI contract surface + OpenAPI export + passing API contract/freshness tests.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-typed-api-foundation/01-CONTEXT.md
@.planning/phases/01-typed-api-foundation/01-RESEARCH.md
@backend/main.py
@backend/run_page/config.py

<interfaces>
From frontend/src/utils/utils.ts:
```typescript
export interface Activity {
  run_id: number;
  name: string;
  distance: number;
  moving_time: string;
  type: string;
  subtype: string;
  start_date: string;
  start_date_local: string;
  location_country?: string | null;
  summary_polyline?: string | null;
  average_heartrate?: number | null;
  elevation_gain: number | null;
  average_speed: number;
  streak: number;
}
```

From backend/run_page/config.py:
```python
SQL_FILE = os.path.join(backend_dir, "run_page", "data.db")
JSON_FILE = os.path.join(root_dir, "data", "activities.json")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create API contract test harness (Wave 0)</name>
  <read_first>@.planning/phases/01-typed-api-foundation/01-RESEARCH.md, @backend/main.py</read_first>
  <files>backend/tests/conftest.py, backend/tests/api/test_contracts.py, backend/tests/api/test_freshness.py, backend/pytest.ini</files>
  <behavior>
    - Test `GET /api/v1/activities` returns 200 and body with `freshness.last_sync_at`, `freshness.completeness`, and `items`.
    - Test `GET /api/v1/analytics/summary` returns 200 and body with required freshness metadata.
    - Test completeness is constrained to explicit states (`complete`, `partial`, `unavailable`).
  </behavior>
  <action>Create backend pytest scaffolding before implementation: add `backend/pytest.ini`, shared TestClient fixture in `backend/tests/conftest.py`, and failing tests in `backend/tests/api/test_contracts.py` + `backend/tests/api/test_freshness.py` that lock `/api/v1` response shape and mandatory freshness fields. Use concrete endpoint paths `/api/v1/activities` and `/api/v1/analytics/summary` and assert keys exactly as `freshness.last_sync_at` and `freshness.completeness` (not optional, not endpoint-specific aliases).</action>
  <verify>
    <automated>cd backend && python -m pytest tests/api/test_contracts.py tests/api/test_freshness.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "def test_activities_contract" backend/tests/api/test_contracts.py` returns one match.
    - `rg "freshness\"\\]\\[\"last_sync_at\"" backend/tests/api/test_freshness.py` returns one or more matches.
    - `rg "/api/v1/analytics/summary" backend/tests/api/test_contracts.py backend/tests/api/test_freshness.py` returns matches.
  </acceptance_criteria>
  <done>Contract tests exist and initially fail against missing API implementation, proving RED baseline for API-01/API-02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `/api/v1` FastAPI contracts with required freshness metadata</name>
  <read_first>@backend/run_page/config.py, @backend/main.py, @backend/tests/api/test_contracts.py, @backend/tests/api/test_freshness.py</read_first>
  <files>backend/api/main.py, backend/api/routers/activities.py, backend/api/routers/analytics.py, backend/api/schemas/common.py, backend/api/schemas/activity.py, backend/api/schemas/analytics.py, backend/api/services/activity_service.py, backend/api/services/analytics_service.py</files>
  <behavior>
    - `/api/v1/activities` returns typed activity list wrapped with deterministic freshness/completeness metadata.
    - `/api/v1/analytics/summary` returns typed analytics summary with the same metadata contract.
    - Missing or unavailable source data returns explicit completeness states instead of ad-hoc null interpretation.
  </behavior>
  <action>Implement FastAPI app and routers under `backend/api` using Pydantic response models as source-of-truth. Create `FreshnessMetadata` in `schemas/common.py` with `last_sync_at: str` and `completeness: Literal["complete","partial","unavailable"]`. Create activity and analytics response models that embed this metadata and typed payloads. Read data from existing local artifacts (`data/activities.json` and/or backend DB helpers) through service modules; if data source is absent, return deterministic empty payload with `completeness="unavailable"` instead of changing schema shape. Mount both routers under `/api/v1` in `backend/api/main.py`. Keep contract additive for v1 and avoid field renames from existing Activity shape.</action>
  <verify>
    <automated>cd backend && python -m pytest tests/api/test_contracts.py tests/api/test_freshness.py -q -x</automated>
  </verify>
  <acceptance_criteria>
    - `rg "APIRouter\\(prefix=\"/api/v1\"" backend/api/main.py backend/api/routers/*.py` returns matches.
    - `rg "Literal\\[\"complete\",\"partial\",\"unavailable\"\\]" backend/api/schemas/common.py` returns one match.
    - `rg "response_model=ActivitiesResponse|response_model=AnalyticsSummaryResponse" backend/api/routers/*.py` returns two matches.
  </acceptance_criteria>
  <done>Backend exposes stable `/api/v1` contract endpoints with mandatory freshness metadata and all contract tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Add deterministic OpenAPI export artifact for frontend type generation</name>
  <read_first>@backend/api/main.py, @frontend/package.json</read_first>
  <files>backend/api/export_openapi.py</files>
  <action>Create `backend/api/export_openapi.py` that imports `app` from `backend/api/main.py`, serializes `app.openapi()` to `backend/api/openapi.json`, and writes UTF-8 JSON with stable indentation. Use this exported file as the canonical input for frontend type generation in Plan 01-02; do not require a running server for typegen.</action>
  <verify>
    <automated>cd backend && python api/export_openapi.py && python -c "import json;json.load(open('api/openapi.json','r',encoding='utf-8'));print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `rg "app.openapi\\(\\)" backend/api/export_openapi.py` returns one match.
    - `Test-Path backend/api/openapi.json` returns `True` after running the verify command.
  </acceptance_criteria>
  <done>OpenAPI contract snapshot can be regenerated locally and parsed successfully for downstream typed frontend generation.</done>
</task>

</tasks>

<verification>
Run `cd backend && python -m pytest tests/api/test_contracts.py tests/api/test_freshness.py -q -x` and verify both API-01 and API-02 tests are green.
</verification>

<success_criteria>
- `/api/v1/activities` and `/api/v1/analytics/summary` return contract-valid JSON with required freshness metadata.
- Completeness values are explicit typed states (`complete|partial|unavailable`) rather than implicit null semantics.
- `backend/api/openapi.json` is generated from backend schemas and ready for frontend codegen.
</success_criteria>

<output>
After completion, create `.planning/phases/01-typed-api-foundation/01-01-SUMMARY.md`
</output>
